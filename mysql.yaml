- name: install mysql configuration
  hosts: mysql
  become: yes
  tasks:
  - name: install mysql
    ansible.builtin.package:
      name: mysql-server
      state: present

  - name: start mysql
    ansible.builtin.service:
      name: mysql-server 
      state: started
      enabled: yes